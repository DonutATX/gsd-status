---
phase: 05-side-panel-treeview
plan: 03
type: execute
wave: 3
depends_on: ["05-02"]
files_modified:
  - package.json
  - resources/gsd-icon.svg
  - src/extension.ts
  - src/test/extension.test.ts
autonomous: true
requirements: [PANL-01, PANL-05, PANL-06]
must_haves:
  truths:
    - "VS Code shows a dedicated GSD Activity Bar view container with a custom SVG icon and a GSD Workflow tree view"
    - "Opening a workspace with no .planning/ shows the welcome view: No GSD project found. Run /gsd:new-project to initialize."
    - "The GSD tree view title bar has a refresh action that triggers StateController.refresh()"
    - "The tree is wired to StateController.onStateChanged and updates when GSD files change"
  artifacts:
    - path: "resources/gsd-icon.svg"
      provides: "24x24 monochrome currentColor Activity Bar icon"
      contains: "currentColor"
    - path: "package.json"
      provides: "viewsContainers, views, viewsWelcome, gsd.refreshTree command, view/title menu"
      contains: "viewsWelcome"
    - path: "src/extension.ts"
      provides: "createTreeView registration, gsd.refreshTree, setContext wiring"
      contains: "createTreeView"
  key_links:
    - from: "src/extension.ts"
      to: "GsdTreeProvider"
      via: "createTreeView + onStateChanged subscription calling provider.update"
      pattern: "createTreeView"
    - from: "package.json viewsWelcome"
      to: "gsd.hasProject context key"
      via: "when clause !gsd.hasProject set by setContext"
      pattern: "gsd.hasProject"
---

<objective>
Contribute the Activity Bar panel and wire `GsdTreeProvider` into the extension so the side panel is visible, refreshable, and shows a welcome view when no GSD project is present.

Purpose: PANL-01 (Activity Bar view container + SVG icon), PANL-05 (welcome view), and PANL-06 (toolbar refresh action) are all declarative `package.json` contributions plus glue code in `activate()`. The tree provider built in plan 02 needs to be registered, fed `GsdState` via `onStateChanged`, and have its welcome-view context key driven by `setContext`.
Output: a `resources/gsd-icon.svg` asset, extended `package.json` contributions, tree registration in `extension.ts`, and a smoke test confirming `gsd.refreshTree` is registered.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-side-panel-treeview/05-RESEARCH.md
@.planning/phases/05-side-panel-treeview/05-PATTERNS.md
@.planning/phases/05-side-panel-treeview/05-UI-SPEC.md

<interfaces>
<!-- GsdTreeProvider API from plan 05-02. -->

From src/tree/provider.ts (after 05-02):
```typescript
export class GsdTreeProvider implements vscode.TreeDataProvider<GsdTreeItem>, vscode.Disposable {
  readonly onDidChangeTreeData: vscode.Event<...>;
  update(state: GsdState): void;
  setRecentCount(n: number): void;
  getTreeItem(element: GsdTreeItem): vscode.TreeItem;
  getChildren(element?: GsdTreeItem): GsdTreeItem[];
  dispose(): void;
}
```

Existing extension.ts wiring points:
- `const folder = vscode.workspace.workspaceFolders?.[0]` — line 19
- `const controller = new StateController(folder)` — line 20, pushed to subscriptions
- Command registration block — lines 44-48 (`gsd.refresh`, `gsd.openRoadmap`, `gsd.openState`)
- `controller.onStateChanged(...)` status-bar subscription — lines 52-74
- `void controller.refresh()` — line 103 (initial refresh; fires the first onStateChanged)

Existing package.json `contributes` has: `commands` (3), `configuration` (gsd.refreshIntervalSeconds, gsd.recentActivityCount).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create the Activity Bar SVG icon and package.json contributions</name>
  <files>resources/gsd-icon.svg, package.json</files>
  <read_first>
    - package.json (existing contributes block — commands, configuration)
    - .planning/phases/05-side-panel-treeview/05-UI-SPEC.md (Package.json Contributions Contract, Activity Bar View Container, Welcome View Contract)
    - .planning/phases/05-side-panel-treeview/05-RESEARCH.md (minimal SVG example, Pitfall 3 id-match, Pitfall 4 SVG format)
  </read_first>
  <action>
    Create the `resources/` directory and `resources/gsd-icon.svg`: a minimal 24x24 monochrome SVG with `xmlns`, `viewBox="0 0 24 24"`, `fill="currentColor"`, a single `<path>`, and NO `width`/`height` attributes and NO hardcoded hex colors (Pitfall 4). Use a checklist/pulse motif per the UI-SPEC.

    Extend the `contributes` object in `package.json` additively — do not remove existing `commands` or `configuration`. Add: `viewsContainers.activitybar` with one entry `{ id: "gsd", title: "GSD", icon: "resources/gsd-icon.svg" }`; `views` keyed by `"gsd"` (must exactly match the container id — Pitfall 3) with one view `{ id: "gsd.treeView", name: "GSD Workflow" }`; `viewsWelcome` with one entry for view `gsd.treeView`, `contents` exactly `No GSD project found. Run \`/gsd:new-project\` to initialize.`, and `when` `!gsd.hasProject`; a fourth command `{ command: "gsd.refreshTree", title: "Refresh GSD tree", icon: "$(refresh)", category: "GSD" }` appended to the `commands` array; and a `menus` object with `view/title` containing `{ command: "gsd.refreshTree", when: "view == gsd.treeView", group: "navigation" }`. Do NOT add an `onView:gsd.treeView` activation event — `onStartupFinished` already covers it (RESEARCH.md State of the Art).
  </action>
  <verify>
    <automated>node -e "const p=require('./package.json');const c=p.contributes;if(!c.viewsContainers.activitybar.find(v=>v.id==='gsd'))throw 'no container';if(!c.views.gsd)throw 'views key mismatch';if(!c.viewsWelcome.find(w=>w.view==='gsd.treeView'))throw 'no welcome';if(!c.commands.find(x=>x.command==='gsd.refreshTree'))throw 'no command';if(!c.menus['view/title'].find(m=>m.command==='gsd.refreshTree'))throw 'no menu';console.log('contributes OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `resources/gsd-icon.svg` exists, is 24x24 viewBox, uses `fill="currentColor"`, has no `width`/`height` and no hex colors
    - `package.json` `contributes` has `viewsContainers.activitybar` (id `gsd`), `views.gsd` (view `gsd.treeView`), `viewsWelcome` (exact message), `gsd.refreshTree` command, and `view/title` menu entry
    - Existing `commands` (3) and `configuration` are preserved
    - The verify node script prints `contributes OK`
  </acceptance_criteria>
  <done>Activity Bar panel, welcome view, and refresh action are declared; SVG icon asset exists.</done>
</task>

<task type="auto">
  <name>Task 2: Wire GsdTreeProvider into extension.ts</name>
  <files>src/extension.ts</files>
  <read_first>
    - src/extension.ts (activate() — command block, onStateChanged subscription, initial refresh)
    - src/tree/provider.ts (GsdTreeProvider API — update, setRecentCount, dispose)
    - .planning/phases/05-side-panel-treeview/05-PATTERNS.md (extension.ts apply-as block, placement rule)
    - .planning/phases/05-side-panel-treeview/05-RESEARCH.md (Pattern 6, Pitfall 2 setContext-on-initial-load)
  </read_first>
  <action>
    In `activate()`, after the existing command-registration block (line 48) and BEFORE `void controller.refresh()` (line 103): import `GsdTreeProvider` from `'./tree/provider.js'`. Construct `const provider = new GsdTreeProvider()`. Read `gsd.recentActivityCount` (default 5) via `vscode.workspace.getConfiguration('gsd', folder?.uri)` and call `provider.setRecentCount(...)`. Call `vscode.window.createTreeView('gsd.treeView', { treeDataProvider: provider, showCollapseAll: false })` and push the returned `TreeView` to `context.subscriptions`; also push `provider` itself (it implements `Disposable`).

    Register `gsd.refreshTree` via `vscode.commands.registerCommand` (handler calls `void controller.refresh()`), pushed to `context.subscriptions`.

    Add a SECOND `controller.onStateChanged` subscription (separate from the existing status-bar one — do not modify that block) that: guards `lifecycle.disposed`; calls `void vscode.commands.executeCommand('setContext', 'gsd.hasProject', state.kind === 'ok')`; and calls `provider.update(state)`. This subscription must be registered before the `void controller.refresh()` call so the initial refresh drives `setContext` on first load (Pitfall 2 — prevents welcome-view flash).

    Extend the existing `onDidChangeConfiguration` listener: when `event.affectsConfiguration('gsd.recentActivityCount', folder?.uri)` is true, re-read the value and call `provider.setRecentCount(...)` then `provider.update(<current state>)` — or, simplest and safe, call `void controller.refresh()` to re-emit state. Keep the existing `gsd.refreshIntervalSeconds` branch unchanged.
  </action>
  <verify>
    <automated>npm run compile</automated>
  </verify>
  <acceptance_criteria>
    - `extension.ts` imports `GsdTreeProvider`, constructs it, registers it via `createTreeView`, and pushes the tree view + provider to `context.subscriptions`
    - `gsd.refreshTree` command is registered and calls `controller.refresh()`
    - A new `onStateChanged` subscription calls `setContext('gsd.hasProject', ...)` and `provider.update(state)`, registered before `controller.refresh()`
    - `gsd.recentActivityCount` config changes propagate to the provider
    - `npm run compile` succeeds
  </acceptance_criteria>
  <done>Tree provider is registered, fed state, and its welcome-view context key is driven by setContext.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Smoke test that gsd.refreshTree is registered</name>
  <files>src/test/extension.test.ts</files>
  <read_first>
    - src/test/extension.test.ts (existing command-registration smoke tests, spyRegisterCommand helper, fakeContext helper)
    - src/extension.ts (activate() after Task 2 — gsd.refreshTree registration)
  </read_first>
  <behavior>
    - After `activate(fakeContext())`, the command map contains `gsd.refreshTree` (PANL-06)
    - The previously registered commands (`gsd.refresh`, `gsd.openRoadmap`, `gsd.openState`) are still present (no regression)
  </behavior>
  <action>
    In `src/test/extension.test.ts`, extend the existing command-registration `describe` block (which uses `spyRegisterCommand`) with an `it` case asserting `gsd.refreshTree` is in the captured command map after `activate()`. Reuse the existing `fakeContext` and spy-restore teardown — do not introduce new harness code. If the file's existing assertion enumerates an expected command list, add `gsd.refreshTree` to it.
  </action>
  <verify>
    <automated>npm test</automated>
  </verify>
  <acceptance_criteria>
    - `extension.test.ts` has an assertion that `gsd.refreshTree` is registered during `activate()`
    - `npm test` is fully green with no regressions
  </acceptance_criteria>
  <done>The refresh-tree command registration is covered by an automated smoke test; full suite passes.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>The complete Phase 5 side panel: an Activity Bar GSD view container with a custom icon, a TreeView listing Recent Activity + all phases (active phase distinguished and expanded), per-phase goal/criteria children, a welcome view for no-project workspaces, and a toolbar refresh button. All automated tests pass.</what-built>
  <how-to-verify>
    1. Run `npm run compile` so `out/` is fresh.
    2. Ensure `../gsd-test/.planning/` has a current ROADMAP.md and a multi-entry STATE.md (per CLAUDE.md Testing & UAT — update the scratch workspace to exercise Phase 5).
    3. Press F5 to launch the Extension Development Host against `../gsd-test`.
    4. Confirm the GSD icon appears in the Activity Bar; click it — the "GSD Workflow" panel opens.
    5. Confirm "Recent Activity" is the first node and lists up to 5 STATE.md entries; the active phase node uses the play icon and is expanded; done phases use the check/pass-filled icon; pending phases use circle-outline.
    6. Expand a phase node — confirm a "Goal: ..." child and one child per success criterion.
    7. Click a phase node — ROADMAP.md opens at that phase's header line. Click a Recent Activity entry — STATE.md opens.
    8. Click the toolbar refresh button — the tree updates; expanded nodes stay expanded (no full collapse).
    9. Open a folder with no `.planning/` — confirm the welcome view shows exactly: "No GSD project found. Run /gsd:new-project to initialize."
  </how-to-verify>
  <resume-signal>Type "approved" or describe any rendering/interaction issues.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `package.json` contributions → VS Code runtime | Declarative manifest evaluated by VS Code (view ids, when-clauses) |
| `gsd.hasProject` context key → `viewsWelcome` when-clause | Extension-set context key drives welcome-view visibility |
| `resources/gsd-icon.svg` → Activity Bar render | Static asset loaded by VS Code |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-06 | Information Disclosure | Welcome view shows incorrectly (project content vs welcome) | mitigate | `setContext('gsd.hasProject', ...)` is called in an `onStateChanged` subscription registered before the initial `controller.refresh()`, so the key is set on first load (RESEARCH.md Pitfall 2). |
| T-05-07 | Elevation of Privilege | `gsd.openRoadmap` receives `headerLine` from a tree node command argument | mitigate | The `gsd.openRoadmap` handler validates `headerLine` is a non-negative integer before any `revealLine` use (RESEARCH.md Security Domain note); if invalid, opens the file without revealing a line. Verified in Task 2 review. |
| T-05-08 | Tampering | `resources/gsd-icon.svg` is loaded by VS Code | accept | SVG is a static authored asset in the repo, monochrome `currentColor`, single `<path>`, no script — no execution surface. |
| T-05-SC | Tampering | npm/pip/cargo installs | mitigate | Zero new packages this plan/phase (RESEARCH.md Package Legitimacy Audit confirms). No install task; nothing to verify. |
</threat_model>

<verification>
- `npm test` is fully green.
- `npm run compile` succeeds.
- `node -e` contributes check prints `contributes OK`.
- `grep -c currentColor resources/gsd-icon.svg` returns ≥ 1.
- `grep -v '^#' src/extension.ts | grep -c "createTreeView"` returns ≥ 1.
- Human verification checkpoint approved.
</verification>

<success_criteria>
- A dedicated GSD Activity Bar view container with a custom SVG icon hosts the "GSD Workflow" tree.
- The welcome view appears for no-project workspaces with the exact required message.
- The tree-title toolbar exposes a refresh action wired to `StateController.refresh()`.
- The tree is fed `GsdState` via `onStateChanged` and `gsd.hasProject` is set on first load.
- Full test suite passes; human verification of the rendered panel is approved.
</success_criteria>

<output>
Create `.planning/phases/05-side-panel-treeview/05-03-SUMMARY.md` when done.
</output>
