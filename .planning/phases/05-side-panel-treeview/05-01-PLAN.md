---
phase: 05-side-panel-treeview
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - src/parsers/types.ts
  - src/parsers/state.ts
  - src/test/parsers/fixtures/multi-entry-state.md
  - src/test/parsers/state.test.ts
autonomous: true
requirements: [PANL-04]
must_haves:
  truths:
    - "parseState returns a recentEntries array when STATE.md body has one or more `Last activity:` lines"
    - "parseState falls back to the frontmatter last_activity for recentEntries[0] when the body has no entries"
    - "The existing lastEntry field is unchanged and still equals recentEntries[0]"
  artifacts:
    - path: "src/parsers/types.ts"
      provides: "recentEntries field on StateData"
      contains: "recentEntries"
    - path: "src/parsers/state.ts"
      provides: "multi-entry collection in parseState body scan"
      contains: "recentEntries"
    - path: "src/test/parsers/fixtures/multi-entry-state.md"
      provides: "STATE.md fixture with multiple body Last activity lines"
  key_links:
    - from: "src/parsers/state.ts"
      to: "StateData.recentEntries"
      via: "body scan pushes buildEntry results into an array"
      pattern: "recentEntries"
---

<objective>
Extend the pure STATE.md parser so it surfaces multiple recent activity entries, not just the single most-recent one. This is the data foundation the Phase 5 TreeView "Recent Activity" section consumes.

Purpose: PANL-04 requires the tree to show the last N STATE.md entries. The parser is the single source of truth for STATE.md data (zero vscode imports), so the multi-entry collection belongs here, not in the provider.
Output: `StateData.recentEntries: StateEntry[]`, an extended `parseState` body scan, a new multi-entry fixture, and unit tests covering canonical (single entry), multi-entry, and frontmatter-fallback inputs.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-side-panel-treeview/05-RESEARCH.md
@.planning/phases/05-side-panel-treeview/05-PATTERNS.md

<interfaces>
<!-- Current StateData and StateEntry. recentEntries is the additive change. -->

From src/parsers/types.ts:
```typescript
export interface StateEntry {
  text: string;
  timestamp?: string;
  raw: string;
}

export interface StateData {
  milestone?: string;
  milestoneName?: string;
  phaseNumber?: string;
  phaseName?: string;
  lastEntry?: StateEntry;
  lastUpdated?: string;
  status?: string;
}
```

From src/parsers/state.ts (existing body scan — extends to collect all matches):
- `LAST_ACT = /^Last activity:\s+(.+?)\s*$/` — regex matching a body activity line
- `buildEntry(raw: string): StateEntry` — constructs a StateEntry, extracting an ISO/date timestamp
- Existing behavior: scan stops at the FIRST `Last activity:` line; falls back to frontmatter `last_activity` when the body has none
</interfaces>
</context>

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1: Add multi-entry fixture and failing recentEntries tests</name>
  <files>src/test/parsers/fixtures/multi-entry-state.md, src/test/parsers/state.test.ts</files>
  <read_first>
    - src/test/parsers/fixtures/canonical-state.md (real STATE.md grammar — confirmed single `Last activity:` body line)
    - src/test/parsers/state.test.ts (existing parseState test structure, fixture loading helper)
    - src/parsers/state.ts (LAST_ACT regex, buildEntry, frontmatter fallback)
  </read_first>
  <behavior>
    - Test 1 (canonical, single body entry): parseState(canonical-state.md) → recentEntries.length === 1, recentEntries[0] deep-equals lastEntry
    - Test 2 (multi-entry body): parseState(multi-entry-state.md) → recentEntries.length === 3, ordered as they appear in the file (first body line is index 0), each has text/raw/timestamp populated
    - Test 3 (lastEntry unchanged): for the multi-entry fixture, lastEntry deep-equals recentEntries[0]
    - Test 4 (frontmatter fallback): a STATE.md text with NO body `Last activity:` line but a frontmatter `last_activity:` value → recentEntries.length === 1, recentEntries[0] deep-equals lastEntry
    - Test 5 (no activity at all): a STATE.md text with neither body nor frontmatter activity → recentEntries is undefined (mirrors lastEntry being undefined)
  </behavior>
  <action>
    Create `multi-entry-state.md` modeled on `canonical-state.md` but with three distinct body `Last activity:` lines, each on its own line with a different ISO-or-date timestamp and message text. Match the EXACT format the `LAST_ACT` regex targets — `Last activity:` followed by whitespace then the entry text. Place the three lines in the body so the first appearing line is the most recent (ordering is positional, no sorting in the parser).

    Add a new `describe('parseState — recentEntries (PANL-04)')` block to `state.test.ts` using the file's existing fixture-loading helper and `strict as assert`. Encode Tests 1-5 above as `it` cases. For Test 4 and Test 5, build the STATE.md text inline (template strings) rather than as fixture files — they are small edge inputs. Run `npm test` and confirm the new cases FAIL (recentEntries does not exist yet) while all pre-existing parser tests still pass.
  </action>
  <verify>
    <automated>npm test 2>&1 | Select-String -Pattern "recentEntries" -Context 0,2</automated>
  </verify>
  <acceptance_criteria>
    - `multi-entry-state.md` exists with exactly 3 body `Last activity:` lines matching the LAST_ACT format
    - New `describe` block with 5 `it` cases is present in `state.test.ts`
    - `npm test` shows the 5 new cases FAILING and zero pre-existing tests regressed
  </acceptance_criteria>
  <done>RED state confirmed: recentEntries tests fail because the field/logic does not exist; all other tests green.</done>
</task>

<task type="tdd" tdd="true">
  <name>Task 2: Add recentEntries to StateData and extend parseState to make tests green</name>
  <files>src/parsers/types.ts, src/parsers/state.ts</files>
  <read_first>
    - src/parsers/types.ts (StateData interface — additive change)
    - src/parsers/state.ts (body scan loop, frontmatter fallback block)
    - src/test/parsers/state.test.ts (the failing recentEntries cases from Task 1)
  </read_first>
  <behavior>
    - All 5 recentEntries cases from Task 1 pass
    - No pre-existing parser test regresses
  </behavior>
  <action>
    In `src/parsers/types.ts`, add an optional `recentEntries?: StateEntry[]` field to the `StateData` interface. Keep `lastEntry` exactly as-is per the CONTEXT.md locked decision (additive only).

    In `src/parsers/state.ts`, replace the single-`bodyLastActivity`-string body scan with a `bodyEntries: StateEntry[]` array: inside the existing line loop, every `LAST_ACT` match pushes `buildEntry(la[1])` onto the array (remove the `bodyLastActivity === undefined` guard so all matches are collected). After the loop: if `bodyEntries.length > 0`, set `data.lastEntry = bodyEntries[0]` and `data.recentEntries = bodyEntries`. Otherwise, in the existing frontmatter `last_activity` fallback branch, after building `data.lastEntry`, also set `data.recentEntries = [data.lastEntry]`. When neither source has activity, leave `recentEntries` undefined (do not assign an empty array) so it mirrors `lastEntry`. Preserve the `POSITION` phase-line handling unchanged and keep the scan single-pass and linear-regex-only.
  </action>
  <verify>
    <automated>npm test</automated>
  </verify>
  <acceptance_criteria>
    - `StateData` declares `recentEntries?: StateEntry[]`
    - `parseState` body scan collects all `Last activity:` lines into an array; `lastEntry === recentEntries[0]` whenever entries exist
    - `npm test` is fully green (all 5 new cases pass, zero regressions)
  </acceptance_criteria>
  <done>GREEN state: the full test suite passes; recentEntries is populated from body entries with a frontmatter fallback.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `.planning/STATE.md` file → parser | Untrusted file content (user-authored or GSD-tool-authored) is read and parsed |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Denial of Service | `parseState` regex on attacker-shaped STATE.md | mitigate | `LAST_ACT` and `ISO_OR_DATE` are linear regexes with no nested quantifiers; collecting all matches is O(lines). PARS-05 stress guard already covers catastrophic backtracking. |
| T-05-02 | Tampering | STATE.md content drives `recentEntries` text | accept | Read-only display; parser never executes or evals content. Entry text is rendered as a plain tree label downstream (no markdown injection path). Low value target, local file. |
| T-05-SC | Tampering | npm/pip/cargo installs | mitigate | Zero new packages this plan/phase (RESEARCH.md Package Legitimacy Audit confirms). No install task; nothing to verify. |
</threat_model>

<verification>
- `npm test` is fully green.
- `grep -v '^#' src/parsers/types.ts | grep -c recentEntries` returns ≥ 1.
- `grep -v '^#' src/parsers/state.ts | grep -c recentEntries` returns ≥ 1.
</verification>

<success_criteria>
- `StateData.recentEntries` exists and is populated by `parseState`.
- Multi-entry STATE.md produces an ordered `recentEntries` array; single-entry and frontmatter-fallback inputs each produce a length-1 array; no-activity input leaves it undefined.
- `lastEntry` behavior is unchanged.
- Full test suite passes with no regressions.
</success_criteria>

<output>
Create `.planning/phases/05-side-panel-treeview/05-01-SUMMARY.md` when done.
</output>
</invoke>
