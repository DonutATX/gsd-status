---
phase: 03-statecontroller-file-watching
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - src/state/types.ts
  - src/state/debounce.ts
  - src/state/controller.ts
  - src/test/state/debounce.test.ts
  - src/test/state/controller.test.ts
  - package.json
autonomous: true
requirements: [WSP-02, WSP-03, WSP-04]
tags: [vscode-extension, state-management, file-parsing]

must_haves:
  truths:
    - "StateController.refresh() reads ROADMAP.md + STATE.md and fires exactly one onStateChanged event"
    - "An I/O or parse failure emits GsdState { kind: 'error' } instead of throwing"
    - "A missing .planning file emits GsdState { kind: 'no-project' }"
    - "debounce coalesces multiple rapid calls within the window into one invocation"
  artifacts:
    - path: "src/state/types.ts"
      provides: "GsdState discriminated union"
      contains: "kind: 'ok'"
    - path: "src/state/debounce.ts"
      provides: "debounce(fn, ms) factory"
      exports: ["debounce"]
    - path: "src/state/controller.ts"
      provides: "StateController class with refresh() and onStateChanged"
      exports: ["StateController"]
    - path: "src/test/state/debounce.test.ts"
      provides: "WAT-02 debounce coalescing tests"
    - path: "src/test/state/controller.test.ts"
      provides: "WSP-02/03/04 controller behavior tests"
  key_links:
    - from: "src/state/controller.ts"
      to: "src/parsers/roadmap.ts"
      via: "parseRoadmap import"
      pattern: "parseRoadmap"
    - from: "src/state/controller.ts"
      to: "vscode.EventEmitter"
      via: "_emitter.fire"
      pattern: "_emitter\\.fire"
---

<objective>
Build the testable core of the reactive state layer: a `GsdState` discriminated
union, a pure `debounce` util, and a `StateController` whose `refresh()` reads both
planning files atomically and fires a single typed change event — with all I/O and
parse errors caught and surfaced as `kind: 'error'` rather than thrown.

Purpose: This is the heart of the Phase 3 vertical slice. Once it exists, the only
remaining work is wiring it to the file watcher and the status bar (Plan 02).
Output: `src/state/{types,debounce,controller}.ts` plus passing unit tests covering
WSP-02, WSP-03, WSP-04, and WAT-02.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-statecontroller-file-watching/03-CONTEXT.md
@.planning/phases/03-statecontroller-file-watching/03-RESEARCH.md
@.planning/phases/03-statecontroller-file-watching/03-PATTERNS.md
@src/parsers/types.ts
@src/parsers/roadmap.ts
@src/parsers/state.ts
@src/test/parsers/stress.test.ts

<interfaces>
<!-- Contracts the executor needs. Already implemented in Phase 2. -->

From src/parsers/types.ts:
```typescript
export interface RoadmapData { projectName?: string; milestoneLabel?: string; phases: RoadmapPhase[]; }
export interface StateData { milestone?: string; milestoneName?: string; phaseNumber?: string; phaseName?: string; lastEntry?: StateEntry; lastUpdated?: string; status?: string; }
```

From src/parsers/roadmap.ts:
```typescript
export function parseRoadmap(text: string): RoadmapData;
```

From src/parsers/state.ts:
```typescript
export function parseState(text: string): StateData;
```

GsdState shape to create (RESEARCH.md Pattern 1):
```typescript
export type GsdState =
  | { kind: 'ok'; roadmap: RoadmapData; state: StateData }
  | { kind: 'no-project' }
  | { kind: 'error'; message: string };
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 0: Wave 0 — scaffold state module + test files, wire test script</name>
  <files>src/state/types.ts, src/state/debounce.ts, src/state/controller.ts, src/test/state/debounce.test.ts, src/test/state/controller.test.ts, package.json</files>
  <read_first>
    - package.json (to extend the `test` and `test:parsers` scripts)
    - src/parsers/types.ts (zero-import pure-types convention to mirror in src/state/types.ts)
    - src/parsers/lines.ts (zero-import pure-util convention to mirror in src/state/debounce.ts)
    - src/test/parsers/stress.test.ts (Mocha + node:assert structure to mirror)
  </read_first>
  <action>
    Create `src/state/types.ts` with the final `GsdState` discriminated union (no stub —
    types have no implementation). Import `RoadmapData` and `StateData` via
    `import type { RoadmapData, StateData } from '../parsers/types.js'`. Zero vscode imports.
    Copy the JSDoc file-header style from `src/parsers/types.ts`.

    Create stub `src/state/debounce.ts` exporting `debounce(fn: () => void, ms: number): () => void`
    that currently just returns `fn` unchanged (RED — real impl in Task 1). Zero imports.

    Create stub `src/state/controller.ts` exporting `class StateController` with a
    `readFiles` injection seam: constructor signature
    `constructor(folder: { uri: { fsPath: string } } | undefined, readFiles?: (base: string) => Promise<{ roadmapText: string; stateText: string }>)`.
    Stub `async refresh(): Promise<void>` (empty) and a public readonly array
    `onStateChangedListeners` plus `dispose(): void` (empty). NOTE: the stub must NOT
    import `vscode` — keep it pure-Node so controller.test.ts runs under bare Mocha
    (RESEARCH.md "Recommended workaround"). The vscode `EventEmitter` is added in Task 2.
    Use a hand-rolled listener array in this plan; Plan 02 swaps it for `vscode.EventEmitter`.

    Create `src/test/state/debounce.test.ts` and `src/test/state/controller.test.ts`
    with failing `describe`/`it` blocks (RED) that import from the new modules — see
    Task 1 and Task 2 `<behavior>` for the cases.

    In `package.json`, change the `test` script glob from
    `mocha "out/test/parsers/**/*.test.js"` to `mocha "out/test/**/*.test.js"` so the
    new `out/test/state/` tests run alongside the 31 parser tests. Leave `test:parsers`
    unchanged.
  </action>
  <verify>
    <automated>npm run compile</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc -p .` exits 0 — all five new files compile under strict mode.
    - `src/state/types.ts` contains `kind: 'ok'`, `kind: 'no-project'`, `kind: 'error'`.
    - `grep -L "import \* as vscode" src/state/controller.ts` succeeds (no vscode import).
    - `package.json` `test` script glob is `out/test/**/*.test.js`.
    - `npm test` discovers and runs the new (failing) state tests — RED state confirmed.
  </acceptance_criteria>
  <done>Five files exist and compile; test script picks up state tests; state tests are RED.</done>
</task>

<task type="tdd" tdd="true">
  <name>Task 1: TDD debounce — coalesce rapid calls (WAT-02)</name>
  <files>src/state/debounce.ts, src/test/state/debounce.test.ts</files>
  <read_first>
    - src/state/debounce.ts (the stub from Task 0)
    - src/test/state/debounce.test.ts (the RED tests from Task 0)
    - src/parsers/lines.ts (JSDoc style and zero-import convention)
  </read_first>
  <behavior>
    - Test 1: three immediate `fn()` calls within a 50ms window result in exactly 1 invocation, checked after 100ms.
    - Test 2: calls at t=0, t=40, t=80 with an 80ms window result in 0 invocations at t=100 and exactly 1 invocation by t=200 (timer resets on each call).
    - Test 3: a single call invokes `fn` exactly once after `ms` elapses.
  </behavior>
  <action>
    RED: ensure `src/test/state/debounce.test.ts` holds the three `<behavior>` cases
    using Mocha's `done` callback for timing (RESEARCH.md "Debounce unit test pattern").
    Commit: `test(03-01): add failing debounce coalescing tests`.

    GREEN: implement `debounce` in `src/state/debounce.ts` per RESEARCH.md Pattern 3 —
    a closure holding `let timer: ReturnType<typeof setTimeout> | undefined`; each call
    clears any pending timer and schedules a fresh one; the scheduled callback clears
    `timer` then invokes `fn`. Zero imports. Commit: `feat(03-01): implement debounce coalescing util`.
  </action>
  <verify>
    <automated>npm test</automated>
  </verify>
  <acceptance_criteria>
    - `npm test` passes including all three `debounce` tests.
    - `src/state/debounce.ts` has zero `import` statements.
    - Two atomic commits exist: a `test(03-01):` commit then a `feat(03-01):` commit.
  </acceptance_criteria>
  <done>debounce coalesces rapid calls to one invocation; all debounce tests green.</done>
</task>

<task type="tdd" tdd="true">
  <name>Task 2: TDD StateController.refresh — atomic dual-file refresh + error-as-state (WSP-02, WSP-03, WSP-04)</name>
  <files>src/state/controller.ts, src/test/state/controller.test.ts</files>
  <read_first>
    - src/state/controller.ts (the stub from Task 0)
    - src/state/types.ts (GsdState union)
    - src/test/state/controller.test.ts (the RED tests from Task 0)
    - src/parsers/roadmap.ts, src/parsers/state.ts (parser API consumed by refresh)
    - src/test/parsers/roadmap.test.ts (describe-per-concern test structure to mirror)
  </read_first>
  <behavior>
    - WSP-03: a `refresh()` with an injected `readFiles` returning valid ROADMAP/STATE text fires exactly ONE event with `kind: 'ok'`, and that event's `roadmap.phases` is non-empty.
    - WSP-02: each `refresh()` call produces exactly one `onStateChanged` notification (counter incremented once per call).
    - WSP-04 (parse path): when `readFiles` resolves but parsed content is unusable, refresh still does not throw — a thrown error from inside refresh is caught and emitted as `kind: 'error'`.
    - WSP-04 (I/O path): when injected `readFiles` rejects with a non-ENOENT error, refresh emits `kind: 'error'` with the error message; it does not throw.
    - no-project path: when injected `readFiles` rejects with an ENOENT error (`code === 'ENOENT'`), refresh emits `kind: 'no-project'`.
    - no-folder path: a StateController constructed with `undefined` folder emits `kind: 'no-project'` on refresh without calling `readFiles`.
  </behavior>
  <action>
    RED: write the six `<behavior>` cases in `src/test/state/controller.test.ts`,
    one `describe` per requirement ID (`WSP-02`, `WSP-03`, `WSP-04`, `no-project`).
    Construct `StateController` with a stub `readFiles` and subscribe via a listener
    helper (see GREEN). Commit: `test(03-01): add failing StateController refresh tests`.

    GREEN: implement `StateController` in `src/state/controller.ts` as a pure-Node class
    (NO vscode import in this plan — Plan 02 introduces the vscode.EventEmitter):
      - Private `_listeners: Array<(s: GsdState) => void> = []`; public
        `onStateChanged(listener): { dispose(): void }` that pushes the listener and
        returns a disposer that splices it out.
      - Private `_emit(state: GsdState)` iterates `_listeners`.
      - Default `readFiles` (used when constructor arg omitted): reads
        `path.join(base, 'ROADMAP.md')` and `path.join(base, 'STATE.md')` via
        `node:fs/promises` with `Promise.all` (RESEARCH.md Pattern 6), where `base`
        is `path.join(folder.uri.fsPath, '.planning')`.
      - `refresh()`: if no folder → `_emit({ kind: 'no-project' })` and return.
        Otherwise wrap in `try/catch`: call `readFiles(base)`, then `parseRoadmap` and
        `parseState`, then `_emit({ kind: 'ok', roadmap, state })`. In `catch`, inspect
        `(err as NodeJS.ErrnoException).code` — `'ENOENT'` → `_emit({ kind: 'no-project' })`,
        otherwise `_emit({ kind: 'error', message: err instanceof Error ? err.message : String(err) })`.
        Exactly one `_emit` per `refresh()` (RESEARCH.md Pattern 7, "no-throw").
      - `dispose()`: clear `_listeners` (watcher/timer wiring added in Plan 02).
    Use `import * as fs from 'node:fs/promises'` and `import * as path from 'node:path'`
    and the `.js` extension on local imports (`./types.js`, `../parsers/roadmap.js`).
    Commit: `feat(03-01): implement StateController atomic refresh with error-as-state`.
  </action>
  <verify>
    <automated>npm test</automated>
  </verify>
  <acceptance_criteria>
    - `npm test` passes — all 31 parser tests plus the new debounce and controller tests green.
    - `grep -c "_emit(" src/state/controller.ts` shows refresh has a single emit per code path (one per branch, not multiple in sequence).
    - `src/state/controller.ts` has zero `import * as vscode` lines.
    - `refresh()` body is wrapped in a single `try/catch`; the catch branch distinguishes ENOENT from other errors.
    - Two atomic commits exist: `test(03-01):` then `feat(03-01):`.
  </acceptance_criteria>
  <done>StateController.refresh() reads both files atomically, fires one typed event, and never throws — WSP-02/03/04 tests green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| disk → StateController | ROADMAP.md / STATE.md file content is untrusted input crossing into the parser |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Tampering | Malformed ROADMAP.md/STATE.md fed to parseRoadmap/parseState | mitigate | Phase 2 PARS-03/PARS-05 already hardened parsers (total, linear regex); StateController additionally wraps the parse call in try/catch so any residual throw becomes `kind: 'error'` rather than crashing the controller |
| T-03-02 | Denial of Service | refresh() throwing inside a fire-and-forget caller | mitigate | refresh() catches all errors internally and emits a state; it never rejects (RESEARCH.md Pattern 7) |
| T-03-03 | Information Disclosure | error message surfaced to UI | accept | Error messages are local fs/parse errors with no secrets; extension reads only two fixed local files |
</threat_model>

<verification>
- `npm test` green: 31 parser tests + 3 debounce tests + 6 controller tests.
- `npx tsc -p .` exits 0 under strict mode.
- No `import * as vscode` in `src/state/types.ts`, `src/state/debounce.ts`, or `src/state/controller.ts`.
</verification>

<success_criteria>
- `GsdState` discriminated union exists and is consumed by `StateController`.
- `debounce` coalesces rapid calls to a single invocation (WAT-02 logic).
- `StateController.refresh()` reads both files atomically and emits exactly one event (WSP-02, WSP-03).
- I/O and parse errors emit `kind: 'error'`; missing files emit `kind: 'no-project'` — no throws (WSP-04).
</success_criteria>

<output>
Create `.planning/phases/03-statecontroller-file-watching/03-01-SUMMARY.md` when done.
</output>
