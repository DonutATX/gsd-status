---
phase: 06-packaging-distribution
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - CHANGELOG.md
  - README.md
  - images/.gitkeep
autonomous: false
requirements: [PKG-04]
must_haves:
  truths:
    - "README.md documents how to install the extension from a .vsix file"
    - "README.md describes the status bar, tooltip, commands, and side panel features"
    - "README.md documents the gsd.refreshIntervalSeconds and gsd.recentActivityCount settings"
    - "README.md includes build-from-source instructions"
    - "README.md references screenshot image files under an images/ directory"
    - "CHANGELOG.md records the v0.1.0 release in Keep a Changelog format"
  artifacts:
    - path: "README.md"
      provides: "Install, feature overview, settings reference, build-from-source docs"
      contains: "code --install-extension"
      min_lines: 40
    - path: "CHANGELOG.md"
      provides: "Keep a Changelog v0.1.0 entry"
      contains: "[0.1.0]"
    - path: "images/.gitkeep"
      provides: "images/ directory for README screenshot files"
  key_links:
    - from: "README.md"
      to: "images/status-bar.png"
      via: "markdown image reference"
      pattern: "images/.*\\.png"
---

<objective>
Replace the placeholder README and add a CHANGELOG so the packaged extension ships real user-facing documentation.

Purpose: VS Code displays README.md and CHANGELOG.md in the extension details panel. The current README still says "Phase 6 will expand this README" — visible to anyone who installs the `.vsix`. PKG-04 requires real install, feature, and settings documentation.
Output: An expanded `README.md`, a new `CHANGELOG.md`, and an `images/` directory for screenshots.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06-packaging-distribution/06-RESEARCH.md
@README.md
@package.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create CHANGELOG.md with the v0.1.0 entry</name>
  <files>CHANGELOG.md</files>
  <read_first>
    - 06-RESEARCH.md "Pattern 3: CHANGELOG.md — Keep a Changelog Format" and "Code Examples — CHANGELOG.md structure" — the target content.
    - package.json — `version` is `0.1.0`; commands and configuration keys to enumerate.
  </read_first>
  <action>
    Create `CHANGELOG.md` at the project root in Keep a Changelog format. Start with a `# Change Log` heading and a one-line note that the file documents all notable changes to the `gsd-status` extension.
    Add a single release section headed `## [0.1.0] - 2026-05-21` with an `### Added` subsection listing every shipped feature: status bar item showing milestone and active phase; hover tooltip with milestone, phase name + goal, and the most recent STATE.md entry; Activity Bar side panel TreeView listing all phases with the active phase highlighted plus a recent activity section; the four commands `gsd.refresh`, `gsd.openRoadmap`, `gsd.openState`, `gsd.refreshTree`; the two settings `gsd.refreshIntervalSeconds` (default 30) and `gsd.recentActivityCount` (default 5); the file watcher on `.planning/ROADMAP.md` and `.planning/STATE.md` with 300ms debounce; the welcome view shown when no GSD project is detected.
    Use the exact `0.1.0` version from `package.json` so the changelog matches the packaged version.
  </action>
  <verify>
    <automated>node -e "const t=require('fs').readFileSync('CHANGELOG.md','utf8'); if(!/\[0\.1\.0\]/.test(t))throw 'no 0.1.0 entry'; if(!/###\s*Added/.test(t))throw 'no Added section'; if(!/status bar/i.test(t))throw 'features not listed'; console.log('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `CHANGELOG.md` exists at the project root.
    - It contains a `## [0.1.0] - 2026-05-21` section with an `### Added` subsection.
    - The Added subsection lists the status bar, tooltip, side panel, the four commands, and the two settings.
  </acceptance_criteria>
  <done>CHANGELOG.md records the v0.1.0 release in Keep a Changelog format.</done>
</task>

<task type="auto">
  <name>Task 2: Expand README.md and create the images/ directory</name>
  <files>README.md, images/.gitkeep</files>
  <read_first>
    - README.md — current placeholder (~12 lines) still references the removed `package:vsix` script and says Phase 6 will expand it.
    - 06-RESEARCH.md "README.md required sections" — the target section list.
    - package.json — `engines.vscode` (`^1.95.0`), command titles, settings descriptions/defaults/minimums, `repository.url`.
  </read_first>
  <action>
    Create the directory `images/` with an empty `.gitkeep` file so the directory is tracked in git (the actual screenshots are captured later as a manual UAT step).
    Rewrite `README.md` with these sections:
    - Title `# GSD Status` and the one-line description.
    - `## Features` — bullet list covering: the status bar item (`$(icon) Milestone › Phase`), the hover tooltip (milestone, phase name + goal, most recent STATE.md entry), the four GSD commands in the Command Palette, and the Activity Bar side panel TreeView (all phases, active phase highlighted, recent activity, welcome view).
    - A screenshot reference: a markdown image pointing to `images/status-bar.png` (and optionally `images/tree-panel.png`) with alt text. The image files do not exist yet — that is expected; capturing them is a manual UAT step.
    - `## Requirements` — VS Code `^1.95.0`.
    - `## Installation` — a "From .vsix (local)" subsection: download `gsd-status-0.1.0.vsix`, then `code --install-extension gsd-status-0.1.0.vsix`, or via Extensions panel → "..." → "Install from VSIX...".
    - `## Configuration` — a table with rows for `gsd.refreshIntervalSeconds` (default 30, min 5) and `gsd.recentActivityCount` (default 5, min 1), each with its description.
    - `## Build from Source` — clone the repo (`https://github.com/DonutATX/gsd-extenstion`), `npm install`, `npm run package`, `code --install-extension gsd-status-0.1.0.vsix`.
    - `## Known Limitations` — read-only (never writes to `.planning/`), single-workspace scope.
    Remove the obsolete `## Scripts` section that references the deleted `package:vsix` script and the "Phase 6 will expand this README" placeholder line.
  </action>
  <verify>
    <automated>node -e "const t=require('fs').readFileSync('README.md','utf8'); for(const s of ['## Features','## Installation','## Configuration','## Build from Source','code --install-extension','gsd.refreshIntervalSeconds','gsd.recentActivityCount']){if(!t.includes(s))throw s+' missing';} if(!/images\/.*\.png/.test(t))throw 'no image ref'; if(/package:vsix|Phase 6 will expand/.test(t))throw 'placeholder remains'; require('fs').accessSync('images/.gitkeep'); console.log('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `images/.gitkeep` exists so the `images/` directory is tracked.
    - `README.md` contains `## Features`, `## Installation`, `## Configuration`, and `## Build from Source` sections.
    - `README.md` documents `code --install-extension`, both settings (`gsd.refreshIntervalSeconds`, `gsd.recentActivityCount`), and references at least one `images/*.png` screenshot.
    - `README.md` no longer contains the `package:vsix` reference or the "Phase 6 will expand" placeholder.
  </acceptance_criteria>
  <done>README.md documents install, features, settings, and build-from-source, and references screenshot files under images/.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>README.md is expanded with install, feature, settings, and build-from-source documentation, CHANGELOG.md records v0.1.0, and the `images/` directory exists. The README references screenshot files that do not yet exist.</what-built>
  <how-to-verify>
    1. Launch the Extension Development Host (F5) against the scratch workspace `../gsd-test`, or install the built `.vsix` from plan 06-01.
    2. Capture a screenshot of the GSD status bar item showing a milestone and phase, and save it to `images/status-bar.png`.
    3. Capture a screenshot of the GSD Activity Bar side panel showing the phase tree and recent activity, and save it to `images/tree-panel.png` (if README references it).
    4. Open `README.md` in a Markdown preview and confirm the screenshot image(s) now render and the install/configuration sections read correctly.
    5. Confirm the image filenames in `images/` exactly match the paths referenced in `README.md`.
  </how-to-verify>
  <resume-signal>Type "approved" once the screenshots are captured into images/ and the README preview renders them correctly, or describe what is missing.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| README/CHANGELOG content → extension details panel | VS Code renders README.md and CHANGELOG.md to any user who installs the extension |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-04 | Information Disclosure | README.md / CHANGELOG.md content | mitigate | README and CHANGELOG contain only public documentation — feature descriptions, install steps, the public GitHub URL. No secrets, paths, or credentials are written (Task 1, Task 2). |
| T-06-05 | Tampering | screenshot image files in `images/` | accept | Screenshots are static PNGs captured manually from the extension UI; they carry no executable content and are reviewed at the human-verify checkpoint before packaging. |
</threat_model>

<verification>
- `node -e` assertions confirm `CHANGELOG.md` has the v0.1.0 entry and `README.md` has all required sections with no placeholder text.
- `images/.gitkeep` exists so the directory is tracked.
- Human verification — screenshots captured into `images/` and README preview renders them.
</verification>

<success_criteria>
- README.md documents install (`code --install-extension <vsix>`), a feature overview, settings reference, and build-from-source instructions (PKG-04).
- README.md references screenshot image files under `images/`, and the directory exists for them (PKG-04).
- CHANGELOG.md records the v0.1.0 release in Keep a Changelog format.
</success_criteria>

<output>
Create `.planning/phases/06-packaging-distribution/06-02-SUMMARY.md` when done.
</output>
