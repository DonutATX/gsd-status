---
phase: 04-tooltip-commands-configuration
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - src/state/relativeTime.ts
  - src/state/tooltip.ts
  - src/extension.ts
  - src/test/state/relativeTime.test.ts
  - src/test/state/tooltip.test.ts
  - src/test/setup/vscode-stub.ts
autonomous: true
requirements: [STAT-03, STAT-04]
must_haves:
  truths:
    - "Hovering the status bar in the ok state shows milestone, active phase name + goal, and the most recent STATE.md entry"
    - "Hovering the status bar in the error state shows the actual parse error message"
    - "Hovering the status bar in the no-project state shows no tooltip"
    - "The last STATE.md entry timestamp renders as relative time with the absolute ISO timestamp beneath it"
  artifacts:
    - path: "src/state/relativeTime.ts"
      provides: "Pure relative-time formatter (zero vscode imports)"
      exports: ["relativeTime"]
    - path: "src/state/tooltip.ts"
      provides: "MarkdownString tooltip builders for ok and error states"
      exports: ["buildOkTooltip", "buildErrorTooltip"]
    - path: "src/extension.ts"
      provides: "Tooltip assignment inside onStateChanged switch; item.command default"
      contains: "item.command"
  key_links:
    - from: "src/extension.ts"
      to: "src/state/tooltip.ts"
      via: "import and call buildOkTooltip / buildErrorTooltip in onStateChanged"
      pattern: "buildOkTooltip|buildErrorTooltip"
    - from: "src/state/tooltip.ts"
      to: "src/state/relativeTime.ts"
      via: "import relativeTime for last-entry formatting"
      pattern: "relativeTime"
---

<objective>
Add the hover tooltip to the GSD status bar item (STAT-03) and assign the status bar's
default command to `gsd.openState` (STAT-04).

Purpose: The status bar currently shows only a one-line summary. Hovering should reveal
the full milestone, active phase + goal, and the most recent STATE.md entry so a developer
gets complete context without opening any file.

Output: A pure `relativeTime()` formatter, a `tooltip.ts` module with `buildOkTooltip` /
`buildErrorTooltip`, the wired `onStateChanged` switch in `extension.ts`, and unit tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-tooltip-commands-configuration/04-RESEARCH.md
@.planning/phases/04-tooltip-commands-configuration/04-PATTERNS.md
@.planning/phases/04-tooltip-commands-configuration/04-UI-SPEC.md

<interfaces>
<!-- Contracts the executor needs. Extracted from the codebase — no exploration required. -->

From src/parsers/types.ts:
```typescript
export interface RoadmapPhase {
  number: string; name: string; goal?: string; mode?: string;
  dependsOn?: string; requirements?: string[]; successCriteria?: string[];
  done: boolean; headerLine: number; endLine: number;
}
export interface RoadmapData {
  projectName?: string; milestoneLabel?: string; phases: RoadmapPhase[];
}
export interface StateEntry { text: string; timestamp?: string; raw: string; }
export interface StateData {
  milestone?: string; milestoneName?: string; phaseNumber?: string;
  phaseName?: string; lastEntry?: StateEntry; lastUpdated?: string; status?: string;
}
```

From src/state/types.ts:
```typescript
export type GsdState =
  | { kind: 'ok'; roadmap: RoadmapData; state: StateData }
  | { kind: 'no-project' }
  | { kind: 'error'; message: string };
```

From src/extension.ts (current onStateChanged switch — modify in place):
- `case 'ok'`: sets `item.text`, currently `item.tooltip = undefined`
- `case 'no-project'`: `item.tooltip = undefined`
- `case 'error'`: `item.tooltip = 'Error parsing GSD files'` (a plain string today)
</interfaces>
</context>

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1: TDD relativeTime() pure formatter</name>
  <files>src/state/relativeTime.ts, src/test/state/relativeTime.test.ts</files>
  <read_first>
    - src/state/debounce.ts (pure-utility module skeleton: file header, single named export, zero imports)
    - src/test/state/controller.test.ts (Mocha describe/it structure, node:assert strict import)
    - .planning/phases/04-tooltip-commands-configuration/04-RESEARCH.md (Pattern 7, Code Examples — full relativeTime implementation)
    - .planning/phases/04-tooltip-commands-configuration/04-UI-SPEC.md (Tooltip Contract — bucket definitions)
  </read_first>
  <behavior>
    - <60s diff → "just now"
    - <60m diff → "Nm ago" (e.g. 5m → "5m ago")
    - <24h diff → "Nh ago" (e.g. 2h → "2h ago")
    - >=24h diff → "N days ago"
    - undefined input → "unknown"
    - empty string input → "unknown"
    - non-parseable string ("not-a-date") → "unknown"
    - future timestamp (negative diff) → "unknown"
  </behavior>
  <action>
    RED first: create `src/test/state/relativeTime.test.ts` importing `relativeTime` from
    `../../state/relativeTime.js` and `strict as assert` from `node:assert`. Write one
    `describe` block per behavior bucket above plus an edge-cases block. Run `npm test` and
    confirm the relativeTime suite fails (module not found).
    GREEN: create `src/state/relativeTime.ts` as a pure module with zero imports — a single
    named export `relativeTime(isoString: string | undefined): string`. Signature and branch
    logic exactly per RESEARCH.md Pattern 7: guard falsy input, compute `Date.now() - new
    Date(isoString).getTime()`, return "unknown" on `isNaN` or negative diff, then floor
    through seconds/minutes/hours/days buckets. No date library — this is the "Don't
    Hand-Roll" exception (5 branches, zero deps).
    Use a `.js` extension on the test's relative import (Node16 module resolution).
  </action>
  <verify>
    <automated>npm test 2>&1 | grep -E "relativeTime" </automated>
  </verify>
  <acceptance_criteria>
    - `src/state/relativeTime.ts` exports `relativeTime` and has zero `import` statements
    - All 8 behavior cases above have a passing `it()` assertion
    - `npm test` passes with the relativeTime suite green
    - No `moment`, `date-fns`, or `luxon` import anywhere in the file
  </acceptance_criteria>
  <done>relativeTime() formatter exists, is pure, and all bucket + edge-case tests pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Tooltip builders + extend vscode-stub</name>
  <files>src/state/tooltip.ts, src/test/state/tooltip.test.ts, src/test/setup/vscode-stub.ts</files>
  <read_first>
    - src/test/setup/vscode-stub.ts (existing stub — extend in place; see PATTERNS.md vscode-stub section)
    - src/test/state/controller.test.ts (Mocha test structure, minimal fixture pattern)
    - .planning/phases/04-tooltip-commands-configuration/04-UI-SPEC.md (Tooltip Contract — exact ok / error / no-project layout and copy)
    - .planning/phases/04-tooltip-commands-configuration/04-RESEARCH.md (Pattern 1 buildOkTooltip / buildErrorTooltip; Pitfall 1 newline rules; Pitfall 5 stub additions)
  </read_first>
  <behavior>
    - buildOkTooltip renders "**Milestone:**", "**Phase:**", "**Goal:**" lines using milestoneLabel ?? projectName ?? "GSD"
    - buildOkTooltip uses the first phase where done === false as the active phase
    - buildOkTooltip renders "All phases complete" when every phase is done
    - buildOkTooltip renders "(no goal defined)" when active.goal is absent
    - buildOkTooltip renders a "Last Entry" section with relativeTime + absolute ISO timestamp + entry text when lastEntry is present
    - buildOkTooltip omits the "Last Entry" section entirely when lastEntry is absent
    - buildErrorTooltip renders "**GSD — Parse Error**" heading followed by the actual message string
    - both builders return a MarkdownString constructed with isTrusted false (default)
  </behavior>
  <action>
    First extend `src/test/setup/vscode-stub.ts`: add a `MarkdownString` class storing
    appended text (`appendMarkdown(value): this`, `get value(): string`) and export it in
    `module.exports`. Follow the existing EventEmitter class style. Do NOT add commands/Uri/
    getConfiguration here — those belong to Plans 02 and 03; keep this change minimal to
    avoid cross-plan stub churn.
    RED: create `src/test/state/tooltip.test.ts` importing `buildOkTooltip` / `buildErrorTooltip`
    from `../../state/tooltip.js`. Build minimal `RoadmapData` / `StateData` fixtures per
    PATTERNS.md tooltip-test section. Assert on the stub MarkdownString's `.value` string for
    each behavior case. Run `npm test`, confirm failure.
    GREEN: create `src/state/tooltip.ts` importing `* as vscode` and `relativeTime` from
    `./relativeTime.js`. Export `buildOkTooltip(roadmap: RoadmapData, state: StateData):
    vscode.MarkdownString` and `buildErrorTooltip(message: string): vscode.MarkdownString`.
    Construct `new vscode.MarkdownString()` — never pass `isTrusted: true` (state.message may
    carry user-controlled path strings; anti-pattern from RESEARCH.md). Use `\n\n` between
    sections and a `---` HR before the Last Entry block per UI-SPEC spacing rules and Pitfall 1.
  </action>
  <verify>
    <automated>npm test 2>&1 | grep -E "tooltip"</automated>
  </verify>
  <acceptance_criteria>
    - `src/test/setup/vscode-stub.ts` exports a `MarkdownString` class with `appendMarkdown` and `value`
    - `src/state/tooltip.ts` exports `buildOkTooltip` and `buildErrorTooltip`
    - No `isTrusted: true` anywhere in `src/state/tooltip.ts`
    - All 8 behavior cases above have a passing `it()` assertion
    - `npm test` passes fully (all suites green)
  </acceptance_criteria>
  <done>Tooltip builders exist, the stub supports MarkdownString, and all tooltip tests pass.</done>
</task>

<task type="auto">
  <name>Task 3: Wire tooltip and default command into extension.ts</name>
  <files>src/extension.ts</files>
  <read_first>
    - src/extension.ts (current activate() and onStateChanged switch — modify in place)
    - src/state/tooltip.ts (buildOkTooltip / buildErrorTooltip signatures from Task 2)
    - .planning/phases/04-tooltip-commands-configuration/04-PATTERNS.md (extension.ts section — switch slots and item.command placement)
    - .planning/phases/04-tooltip-commands-configuration/04-UI-SPEC.md (Status Bar Item Contract)
  </read_first>
  <action>
    Import `buildOkTooltip` and `buildErrorTooltip` from `./state/tooltip.js` (`.js` extension
    required, Node16). In the `onStateChanged` switch: `case 'ok'` set
    `item.tooltip = buildOkTooltip(state.roadmap, state.state)`; `case 'error'` set
    `item.tooltip = buildErrorTooltip(state.message)`; `case 'no-project'` leave
    `item.tooltip = undefined`.
    Set `item.command = 'gsd.openState'` once during `activate()`. Per STAT-04 / CONTEXT.md
    "what just happened" is the primary glance value. NOTE: the `gsd.openState` command itself
    is registered in Plan 02 — assigning `item.command` to an as-yet-unregistered command is
    safe (VS Code resolves it lazily on click); place this assignment after the status bar
    item is created. Do not register any command in this plan.
  </action>
  <verify>
    <automated>npm test 2>&1 | grep -E "passing" && npx tsc -p . --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `src/extension.ts` imports `buildOkTooltip` and `buildErrorTooltip` from `./state/tooltip.js`
    - `case 'ok'` assigns `buildOkTooltip(...)` to `item.tooltip`; `case 'error'` assigns `buildErrorTooltip(...)`
    - `item.command` is set to the string `'gsd.openState'`
    - `npx tsc -p . --noEmit` reports zero errors
    - `npm test` passes fully
  </acceptance_criteria>
  <done>Status bar hover shows the rich tooltip; clicking the item targets gsd.openState.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| disk → extension | `.planning/STATE.md` and `ROADMAP.md` contents are parsed and rendered into the tooltip; file contents are untrusted input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Tampering | tooltip.ts MarkdownString rendering | mitigate | Construct `MarkdownString` without `isTrusted: true` so a malicious STATE.md cannot inject a command URI that executes on hover |
| T-04-02 | Information Disclosure | buildErrorTooltip(message) | accept | Error message may echo a local filesystem path; acceptable — the path is the developer's own workspace, no remote exposure, read-only extension |
| T-04-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs in this plan — zero new dependencies; nothing to verify |
</threat_model>

<verification>
- `npm test` — all suites green (relativeTime, tooltip, plus existing parser/controller suites)
- `npx tsc -p . --noEmit` — zero compile errors
- Manual EDH check (deferred to phase gate): hover the status bar item in `../gsd-test`, confirm milestone / phase / goal / last-entry render
</verification>

<success_criteria>
- relativeTime() formats all four buckets and returns "unknown" for invalid input
- Tooltip ok state shows milestone, active phase + full goal, and the last STATE.md entry with relative + absolute timestamp
- Tooltip error state shows the actual parse error message
- Tooltip no-project state is undefined
- item.command equals 'gsd.openState'
</success_criteria>

<output>
Create `.planning/phases/04-tooltip-commands-configuration/04-01-SUMMARY.md` when done
</output>
