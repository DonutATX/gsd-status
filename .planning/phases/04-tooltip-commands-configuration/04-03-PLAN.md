---
phase: 04-tooltip-commands-configuration
plan: 03
type: execute
wave: 3
depends_on: ["04-02"]
files_modified:
  - package.json
  - src/state/controller.ts
  - src/extension.ts
  - src/test/state/controller.test.ts
  - src/test/setup/vscode-stub.ts
autonomous: true
requirements: [CFG-01, CFG-02, CFG-03]
must_haves:
  truths:
    - "gsd.refreshIntervalSeconds and gsd.recentActivityCount appear in VS Code settings with declared defaults and minimums"
    - "Changing gsd.refreshIntervalSeconds restarts the periodic refresh timer without a window reload"
    - "An out-of-range refresh interval is clamped to a minimum of 5 seconds in code"
  artifacts:
    - path: "package.json"
      provides: "contributes.configuration declaring two gsd.* settings"
      contains: "gsd.refreshIntervalSeconds"
    - path: "src/state/controller.ts"
      provides: "setRefreshInterval(seconds) method that replaces the periodic timer"
      contains: "setRefreshInterval"
    - path: "src/extension.ts"
      provides: "onDidChangeConfiguration listener that calls controller.setRefreshInterval"
      contains: "onDidChangeConfiguration"
  key_links:
    - from: "src/extension.ts"
      to: "src/state/controller.ts"
      via: "onDidChangeConfiguration listener calls controller.setRefreshInterval(seconds)"
      pattern: "setRefreshInterval"
    - from: "src/extension.ts"
      to: "package.json gsd.refreshIntervalSeconds"
      via: "getConfiguration('gsd').get('refreshIntervalSeconds') reads the contributed setting"
      pattern: "getConfiguration"
---

<objective>
Add two VS Code settings — `gsd.refreshIntervalSeconds` and `gsd.recentActivityCount` — and
make `gsd.refreshIntervalSeconds` apply live by restarting the periodic refresh timer
without a window reload (CFG-01, CFG-02, CFG-03).

Purpose: A developer should be able to tune the refresh cadence from VS Code Settings and
see it take effect immediately. `gsd.recentActivityCount` is declared now (Phase 5 consumes
it) so the manifest schema is complete.

Output: `contributes.configuration` in `package.json`, a `setRefreshInterval(seconds)`
method on `StateController`, an `onDidChangeConfiguration` listener in `activate()`, and
controller timer-restart tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-tooltip-commands-configuration/04-RESEARCH.md
@.planning/phases/04-tooltip-commands-configuration/04-PATTERNS.md
@.planning/phases/04-tooltip-commands-configuration/04-UI-SPEC.md

<interfaces>
<!-- Contracts the executor needs. Extracted from the codebase. -->

From src/state/controller.ts (current — modify in place):
```typescript
export class StateController implements vscode.Disposable {
  // line 42: declared `private readonly _timerDisposable` — MUST drop `readonly`
  private readonly _timerDisposable: vscode.Disposable;
  constructor(
    folder: vscode.WorkspaceFolder | { uri: { fsPath: string } } | undefined,
    readFiles?: (base: string) => Promise<{ roadmapText: string; stateText: string }>,
  );
  async refresh(): Promise<void>;
  dispose(): void;   // calls this._timerDisposable.dispose()
}
```
Current timer setup (constructor lines 61-78): `safeRefresh` closure +
`setInterval(safeRefresh, REFRESH_INTERVAL_MS)` wrapped as
`{ dispose: () => clearInterval(id) }`.

package.json `contributes` currently holds only `commands` (added by Plan 02).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Declare configuration in package.json + extend vscode-stub</name>
  <files>package.json, src/test/setup/vscode-stub.ts</files>
  <read_first>
    - package.json (current — `contributes` has `commands` from Plan 02)
    - src/test/setup/vscode-stub.ts (existing stub — extend in place)
    - .planning/phases/04-tooltip-commands-configuration/04-PATTERNS.md (package.json contributes.configuration block; vscode-stub section)
    - .planning/phases/04-tooltip-commands-configuration/04-UI-SPEC.md (Configuration Contract — exact types, defaults, minimums, descriptions, scope)
  </read_first>
  <action>
    In `package.json` `contributes`, add a `configuration` object alongside the existing
    `commands` array: `title: "GSD Status"`, `properties` with two entries.
    `gsd.refreshIntervalSeconds`: `type: "number"`, `default: 30`, `minimum: 5`, `scope:
    "window"`, description "Interval in seconds between automatic GSD file refreshes."
    `gsd.recentActivityCount`: `type: "number"`, `default: 5`, `minimum: 1`, `scope:
    "window"`, description "Number of recent STATE.md entries to surface in the GSD side
    panel." Values must match UI-SPEC exactly.
    Extend `src/test/setup/vscode-stub.ts`: add `workspace.getConfiguration(section)`
    returning an object with `get(key, defaultValue)` that returns `defaultValue`; add
    `workspace.onDidChangeConfiguration(listener)` returning a no-op disposable. Follow the
    existing `workspace` export structure (PATTERNS.md vscode-stub section).
  </action>
  <verify>
    <automated>node -e "const p=require('./package.json'); const c=p.contributes.configuration.properties; if(c['gsd.refreshIntervalSeconds'].minimum!==5||c['gsd.recentActivityCount'].default!==5) process.exit(1)" && npm run compile</automated>
  </verify>
  <acceptance_criteria>
    - `package.json` `contributes.configuration` declares both `gsd.refreshIntervalSeconds` and `gsd.recentActivityCount`
    - `gsd.refreshIntervalSeconds`: default 30, minimum 5, scope "window"
    - `gsd.recentActivityCount`: default 5, minimum 1, scope "window"
    - `contributes.commands` from Plan 02 is still present (not overwritten)
    - `vscode-stub.ts` exports `workspace.getConfiguration` and `workspace.onDidChangeConfiguration`
    - `npm run compile` succeeds
  </acceptance_criteria>
  <done>Both settings are declared in the manifest; the stub supports configuration APIs.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add StateController.setRefreshInterval()</name>
  <files>src/state/controller.ts, src/test/state/controller.test.ts</files>
  <read_first>
    - src/state/controller.ts (current — `_timerDisposable` is `readonly`; timer built in constructor)
    - src/test/state/controller.test.ts (existing controller tests — add to this file)
    - .planning/phases/04-tooltip-commands-configuration/04-RESEARCH.md (Pattern 6 + Code Examples setRefreshInterval; Pitfall 2 readonly; Open Question 2 dispose interaction)
    - .planning/phases/04-tooltip-commands-configuration/04-PATTERNS.md (controller.ts section — field mutability, timer construction, new method)
  </read_first>
  <behavior>
    - setRefreshInterval(10) replaces the running interval (old timer cleared, new timer started)
    - setRefreshInterval(2) clamps to a 5-second minimum (Math.max(5, seconds))
    - setRefreshInterval(0) clamps to a 5-second minimum
    - calling setRefreshInterval after dispose() does not start a leaked timer
    - dispose() still clears whatever timer is current after setRefreshInterval was called
  </behavior>
  <action>
    Change line 42 from `private readonly _timerDisposable` to `private _timerDisposable`
    so it can be reassigned (Pitfall 2 — TypeScript errors otherwise).
    Add a `private _disposed = false;` field; set it to `true` inside `dispose()` (Open
    Question 2 — guard against a post-dispose timer leak).
    Add public `setRefreshInterval(seconds: number): void`: if `_disposed`, return early.
    Otherwise `const ms = Math.max(5, seconds) * 1000` (defensive clamp — CFG guard), call
    `this._timerDisposable.dispose()` to clear the old interval, build a `safeRefresh`
    closure identical to the constructor's (`this.refresh().catch(...)`), `setInterval`, and
    reassign `this._timerDisposable = { dispose: () => clearInterval(id) }`.
    Do NOT make the controller call `vscode.workspace.getConfiguration` — that coupling
    breaks the Mocha stub (RESEARCH.md anti-pattern). The initial interval stays the
    constructor's existing `REFRESH_INTERVAL_MS`; `activate()` will push live changes via
    `setRefreshInterval`. No constructor signature change is needed.
    Add `describe('setRefreshInterval', ...)` cases to `controller.test.ts` covering all 5
    behaviors. Use Mocha fake timers or assert on observable disposal — keep tests fast.
  </action>
  <verify>
    <automated>npm test 2>&1 | grep -E "setRefreshInterval|passing" && npx tsc -p . --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `_timerDisposable` is declared `private` without `readonly`
    - `StateController` has a `_disposed` boolean set true in `dispose()`
    - `setRefreshInterval(seconds)` clamps via `Math.max(5, seconds)`, disposes the old timer, and starts a new one
    - `setRefreshInterval` returns early when `_disposed` is true
    - All 5 behavior cases have passing `it()` assertions in `controller.test.ts`
    - `npx tsc -p . --noEmit` reports zero errors; `npm test` passes fully
  </acceptance_criteria>
  <done>StateController can restart its periodic timer at a clamped interval and is leak-safe after dispose.</done>
</task>

<task type="auto">
  <name>Task 3: Wire onDidChangeConfiguration listener in activate()</name>
  <files>src/extension.ts</files>
  <read_first>
    - src/extension.ts (current — Plans 01 and 02 already added tooltip + commands; modify in place)
    - src/state/controller.ts (final — setRefreshInterval signature from Task 2)
    - .planning/phases/04-tooltip-commands-configuration/04-RESEARCH.md (Pattern 5 live config reload; Pitfall 6 fully-qualified affectsConfiguration key)
    - .planning/phases/04-tooltip-commands-configuration/04-PATTERNS.md (extension.ts config listener pattern)
  </read_first>
  <action>
    In `activate()`, register a `vscode.workspace.onDidChangeConfiguration` listener and push
    its disposable to `context.subscriptions`. Inside the listener: if
    `event.affectsConfiguration('gsd.refreshIntervalSeconds')` — use the FULLY-QUALIFIED key,
    not `'gsd'` (Pitfall 6, otherwise a `recentActivityCount` change spuriously restarts the
    timer) — read `vscode.workspace.getConfiguration('gsd').get<number>('refreshIntervalSeconds',
    30)` and call `controller.setRefreshInterval(seconds)`.
    `gsd.recentActivityCount` needs no live action in Phase 4 (Phase 5 consumes it) — add a
    short comment noting that and take no action in this branch.
    Honor the existing `lifecycle.disposed` guard pattern if a late event could fire after
    disposal: the controller's own `_disposed` guard (Task 2) already covers the timer, so a
    simple early-return on `lifecycle.disposed` is sufficient and consistent with the
    onStateChanged handler.
  </action>
  <verify>
    <automated>npx tsc -p . --noEmit && npm test 2>&1 | grep -E "passing"</automated>
  </verify>
  <acceptance_criteria>
    - `src/extension.ts` registers `vscode.workspace.onDidChangeConfiguration` and pushes the disposable to `context.subscriptions`
    - The listener filters with the fully-qualified key `'gsd.refreshIntervalSeconds'`
    - On a matching change it reads the new value via `getConfiguration('gsd').get` and calls `controller.setRefreshInterval`
    - `npx tsc -p . --noEmit` reports zero errors; `npm test` passes fully
  </acceptance_criteria>
  <done>Changing the refresh interval setting restarts the periodic timer live, with no window reload.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user/settings → extension | A developer (or a malicious workspace `.vscode/settings.json`) supplies the numeric config values that drive the refresh timer |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-05 | Denial of Service | setRefreshInterval / refreshIntervalSeconds | mitigate | `package.json` `minimum: 5` plus a `Math.max(5, value)` clamp in `setRefreshInterval` prevents a 0s interval busy-loop that would spin the extension host |
| T-04-06 | Denial of Service | setRefreshInterval after dispose | mitigate | `_disposed` guard returns early so a config change cannot resurrect a leaked timer on a disposed controller |
| T-04-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs in this plan — zero new dependencies; nothing to verify |
</threat_model>

<verification>
- `node -e` package.json assertion — both settings declared with correct defaults/minimums
- `npx tsc -p . --noEmit` — zero compile errors
- `npm test` — all suites green including the new `setRefreshInterval` cases
- Manual EDH check (phase gate): change `gsd.refreshIntervalSeconds` in Settings, confirm the timer restarts without a window reload
</verification>

<success_criteria>
- Both `gsd.refreshIntervalSeconds` and `gsd.recentActivityCount` appear in VS Code settings with declared schema
- `setRefreshInterval` clamps to a 5-second minimum and replaces the running timer
- Changing the interval setting applies live via `onDidChangeConfiguration` — no window reload
- A post-dispose config change does not leak a timer
</success_criteria>

<output>
Create `.planning/phases/04-tooltip-commands-configuration/04-03-SUMMARY.md` when done
</output>
